version: 2
seeds:
  - name: divisions
    config:
      schema: "{{env_var('dbt_seedschema','lookup')}}"
  - name: categories
    config:
      schema: "{{env_var('dbt_seedschema','lookup')}}"
  - name: shippers
    config:
      schema: "{{env_var('dbt_seedschema','lookup')}}"







