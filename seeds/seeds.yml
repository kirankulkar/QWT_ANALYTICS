version: 2
seeds:
  - name: divisions
    config:
      schema: "{{env_var('DBT_SEEDSSCHEMA','lookup')}}"
  - name: categories
    config:
      schema: "{{env_var('DBT_SEEDSSCHEMA','lookup')}}"
  - name: shippers
    config:
      schema: "{{env_var('DBT_SEEDSSCHEMA','lookup')}}"







